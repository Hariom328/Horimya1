const playlistSchema = require('../models/playlist');

module.exports = {
	name: 'saveplaylist',
	aliases: ['sp', 'saveplaylist'],
	description: 'Save your playlist and play by just one keyword',
	cooldown: 2,
	//args: true/false,
	//usage: '',
	//permission: '',
	// guildOnly: true/false,
	execute(message, args) {
        
	},
};